<h1 class = "text-center rental-title mb-3 mt-3 request" id = "rental-requests">Rental Requests</h1>
<div class = "my-offers">
  <div class="text-center">
    <% if @requests.empty? %>
      <p>You haven't made any requests yet.</p>
    <% else %>
  </div>
      <% @requests&.each do |request| %>
          <div class="card w-50 shadow p-3  mb-3 rental-requests-card">
            <div class="card-body">
              <h5 class="card-title"><%= request.offer.item_name %></h5>
              <div class = "row">
              <div class = "col">
                <% if request.offer.photo.key.present? %>
                  <%= cl_image_tag request.offer.photo.key, height: 200, width: 200 %>
                <% end %>
                </div>
                <div class = "col">
              <ul class="card-text-rental mt-3">
              <% if request.start_date %>
                <li class = "margin-li">Total price: <%= number_to_currency(((request.end_date - request.start_date) / 86400) * request.offer.rate, { unit: "¥", precision: 0 }) %></li>
                <li class = "margin-li">Rental period: <%= request.start_date.strftime("%b %d, %Y") %> to <%= request.end_date.strftime("%b %d, %Y") %></li>
                <% end %>
                <li>Status: <%= request.status %></li>
              </ul>
              </div>
            </div>
          </div>
        </div>

      <% end %>
    <% end %>

</div>
